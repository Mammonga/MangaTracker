<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book Entry</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Edit Book Entry</h1>
        </header>
        <section class="form-section">
            <form action="/books/<%= book.id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <label for="title">Title:</label>
                    <input type="text" id="title" name="title" value="<%= book.title %>" required>
                </div>
                <div class="form-group">
                    <label for="author">Author:</label>
                    <input type="text" id="author" name="author" value="<%= book.author %>" required>
                </div>
                <div class="form-group">
                    <label for="genre">Genre:</label>
                    <input type="text" id="genre" name="genre" value="<%= book.genre %>" required>
                </div>
                <div class="form-group">
                    <label for="rating">Rating:</label>
                    <input type="text" id="rating" name="rating" value="<%= book.rating %>" required>
                </div>
                <div class="form-group">
                    <label for="review">Review:</label>
                    <textarea id="review" name="review" rows="5" required><%= book.review %></textarea>
                </div>
                <div class="form-group">
                    <label for="cover_url">Cover URL (ISBN):</label>
                    <input type="text" id="cover_url" name="cover_url" value="<%= book.cover_url %>" required>
                </div>
                <div class="form-group">
                    <label for="status">Status:</label>
                    <select id="status" name="status" value="<%= book.status %>">
                        <option value="completed" <%= book.status === 'completed' ? 'selected' : '' %>>Completed</option>
                        <option value="reading" <%= book.status === 'reading' ? 'selected' : '' %>>Reading</option>
                        <option value="plan_to_read" <%= book.status === 'plan_to_read' ? 'selected' : '' %>>Plan to Read</option>
                    </select>
                </div>
                <div class="form-group">
                    <button type="submit">Save Changes</button>
                </div>
            </form>
            <a href="/" class="button back-button">Back to Books List</a>
        </section>
    </div>
</body>
</html>
